<UserControl x:Class="AdbControl.Tools.Console.Views.AdbConsoleToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="ConsoleOutputTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxInputStyle}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="ConsoleInputTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxInputStyle}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Border Style="{DynamicResource PanelSurfaceStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Text="{Binding EmptyStateMessage}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource Text.BodyMutedStyle}">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasOutput}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBox x:Name="OutputTextBox"
                             Text="{Binding OutputText, Mode=OneWay}"
                             TextChanged="OnOutputTextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource ConsoleOutputTextBoxStyle}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasOutput}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>

                <Border Grid.Row="2"
                        Padding="8,6"
                        Background="{DynamicResource Brush.SurfaceAlt}"
                        BorderBrush="{DynamicResource Brush.BorderDefault}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource Radius.Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock VerticalAlignment="Center"
                                   FontFamily="Consolas"
                                   FontSize="13"
                                   Foreground="{DynamicResource Brush.TextSecondary}"
                                   Text="&gt;" />

                        <TextBox x:Name="CommandInputTextBox"
                                 Grid.Column="1"
                                 Margin="8,0,0,0"
                                 ToolTip="Команда adb"
                                 VerticalContentAlignment="Center"
                                 PreviewKeyDown="OnCommandInputPreviewKeyDown"
                                 Style="{StaticResource ConsoleInputTextBoxStyle}"
                                 Text="{Binding CommandInput, UpdateSourceTrigger=PropertyChanged}" />

                        <Button Grid.Column="2"
                                Margin="8,0,0,0"
                                Command="{Binding ClearOutputCommand}"
                                Content="Очистить"
                                Style="{DynamicResource SecondaryButtonStyle}" />

                        <Button Grid.Column="3"
                                Margin="8,0,0,0"
                                Command="{Binding StopCommand}"
                                Content="Стоп"
                                Style="{DynamicResource DangerButtonStyle}" />
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
