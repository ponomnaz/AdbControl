<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="PanelSurfaceStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.SurfacePrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{DynamicResource Radius.Panel}" />
        <Setter Property="Padding" Value="{DynamicResource Padding.Panel}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="PanelSurfaceAltStyle" TargetType="Border" BasedOn="{StaticResource PanelSurfaceStyle}">
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceAlt}" />
    </Style>

    <Style x:Key="PanelInsetStyle" TargetType="Border" BasedOn="{StaticResource PanelSurfaceStyle}">
        <Setter Property="Background" Value="{DynamicResource Brush.BackgroundSecondary}" />
        <Setter Property="Padding" Value="{DynamicResource Padding.PanelDense}" />
    </Style>

    <Style x:Key="IconChipStyle" TargetType="Border">
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="28" />
        <Setter Property="CornerRadius" Value="{DynamicResource Radius.Control}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceAlt}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
</ResourceDictionary>
